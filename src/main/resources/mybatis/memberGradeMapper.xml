<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mg">
	<insert id="insert">
		INSERT INTO member_grade(m_no, mg_date, mg_grade, mg_point, mg_reason)
		VALUES(${m_no}, sysdate, #{mg_grade}, #{mg_point}, #{mg_reason})
	</insert>
</mapper>