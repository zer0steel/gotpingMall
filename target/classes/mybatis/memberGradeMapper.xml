<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mg">
	<insert id="insert">
		INSERT INTO member_grade(m_no, mg_date, grade, point, reason)
		VALUES(${m_no}, sysdate, #{grade}, #{point}, #{reason})
	</insert>
</mapper>