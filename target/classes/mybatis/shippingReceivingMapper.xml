<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sr">
	<select id="selectAll" resultType="com.got.vo.goods.ShippingReceivingVO">
		SELECT * 
		FROM goods g, shipping_receiving sr 
		WHERE g.g_no = sr.g_no ORDER BY regdate DESC
	</select>
	
	<select id="selectWithG_no" resultType="com.got.vo.goods.ShippingReceivingVO">
		SELECT r.* FROM (SELECT * FROM shipping_receiving WHERE g_no = #{g_no} ORDER BY regdate DESC) r
		<if test="count != null">
			<![CDATA[WHERE ROWNUM <= #{count}]]>
		</if>
	</select>
	
	<insert id="insertOne">
		INSERT INTO shipping_receiving(sr_no, g_no, category, detail, amount, price, regdate, change_stock)
		VALUES(sr_no.nextval, #{g_no}, #{category}, #{detail}, #{amount}, #{price}, sysdate, #{change_stock})
	</insert>
</mapper>